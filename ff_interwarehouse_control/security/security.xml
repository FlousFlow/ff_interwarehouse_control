<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>

        <record id="group_interwarehouse_manager" model="res.groups">
            <field name="name">Inter-Warehouse / Global Warehouse Manager</field>
            <field name="category_id" ref="base.module_category_inventory_inventory"/>
            <field name="implied_ids" eval="[(4, ref('stock.group_stock_manager'))]"/>
            <field name="comment">Sees all warehouses (inside their allowed companies), manages lanes, resolves transit differences and edits warehouse access of users.</field>
        </record>

        <record id="group_restricted_warehouse_user" model="res.groups">
            <field name="name">Inter-Warehouse / Restricted Warehouse User</field>
            <field name="category_id" ref="base.module_category_inventory_inventory"/>
            <field name="comment">A stock user with this group only sees the stock of the warehouses listed in their "Allowed Warehouses" (enforced with record rules, not view domains). Fail-closed: empty allowed list = no warehouse stock visible.</field>
        </record>

    </data>
</odoo>
